"use client";

import { memo } from "react";

type HeaderProps = {
  market?: string;
};

function HeaderComponent({ market }: HeaderProps) {
  return null;
}

export default memo(HeaderComponent);
